_schema-version: "3.2"
ID: tbsplppaplanning
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: tbsplppaplanning-destination-content
  type: com.sap.application.content
  requires:
  - name: tbsplppaplanning-destination-service
    parameters:
      content-target: true
  - name: tbsplppaplanning-repo-host
    parameters:
      service-key:
        name: tbsplppaplanning-repo-host-key
  - name: tbsplppaplanning-uaa
    parameters:
      service-key:
        name: tbsplppaplanning-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tbsplppaplanning_html_repo_host
          ServiceInstanceName: tbsplppaplanning-html5-srv
          ServiceKeyName: tbsplppaplanning-repo-host-key
          sap.cloud.service: tbsplppaplanning
        - Authentication: OAuth2UserTokenExchange
          Name: tbsplppaplanning_uaa
          ServiceInstanceName: tbsplppaplanning-xsuaa-srv
          ServiceKeyName: tbsplppaplanning-uaa-key
          sap.cloud.service: tbsplppaplanning
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tbsplppaplanning-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tbsplppaplanning-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tbsplppaplanning.zip
      name: tbsplppaplanning
      target-path: resources/
- name: tbsplppaplanning
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tbsplppaplanning-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tbsplppaplanning-destination-service
    service-plan: lite
- name: tbsplppaplanning-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tbsplppaplanning-xsuaa-srv
    service-plan: application
- name: tbsplppaplanning-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tbsplppaplanning-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
